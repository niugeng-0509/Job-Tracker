# AI Job Copilot 协作规则

本文件只规定 Codex 在这个项目中如何检查、教学、修改和验证。项目当前进度看 `docs/CURRENT_PROGRESS.md`，长期路线看 `docs/PROJECT_ROADMAP.md`，详细知识与架构看 `docs/AI_JOB_COPILOT_LEARNING_GUIDE.md`。

## 新对话开始

先进行只读检查：

1. 阅读 `docs/CURRENT_PROGRESS.md`。
2. 检查 `git status` 和最近的 `git log`。
3. 检查当前实际代码、测试和配置。
4. 按需阅读路线图中的当前里程碑。
5. 只阅读学习指南中与当前任务有关的章节。
6. 运行与当前任务风险相称的验证。

判断进度的优先级：

```text
实际代码和测试
> Git 历史
> CURRENT_PROGRESS.md
> 路线图日期
```

文档与代码不一致时，以实际代码和测试为准，并指出差异。前一里程碑没有通过验收时，不因日期到了而跳到下一阶段。

## 每日学习任务

用户询问“今天做什么”时，先说明：

- 当前阶段
- 已完成且验证通过的内容
- 尚未完成的内容
- 今天选择该任务的原因

然后提供：

- 今日学习目标和关键概念
- 预计 5-7 小时安排
- 按顺序实施的步骤
- 需要用户手动输入的代码
- 每段代码的输入、输出、调用关系和所属层
- 正常路径、异常路径和常见错误
- 验证方法、验收标准和建议提交信息

## 默认学习模式

用户希望通过亲自输入代码学习。

除非用户明确要求直接修改文件，否则：

- 将代码写在对话中，不直接实现。
- 把功能拆成可逐段输入和验证的小步骤。
- 先解释数据流和职责，再解释语法。
- 不假设用户已经掌握尚未学习的框架。

用户明确要求修改、创建、验证、提交或推送时，可以执行对应操作。

## 完成与证据

只有满足以下条件，才能把任务写成“已完成”：

- 代码已保存。
- 正常路径验证通过。
- 关键异常路径验证通过。
- 有自动测试；尚未学习测试时必须注明“仅手工验证”。
- 文档与实现一致。

不得编造覆盖率、RAG 指标、延迟、Token 成本或性能提升。这些数字只能来自实际命令和评测。

## 修改与 Git

- 修改前检查 `git status`，保留用户已有改动。
- 默认不提交 `resume_output/`、`tools/` 和本地 `data/jobs.json`。
- 提交前运行相关检查并使用精确的 `git add` 路径。
- 未经用户要求，不创建提交或推送远程。
- 不把计划中的功能描述成已经实现。

## 每天结束

用户要求维护进度时：

1. 检查实际代码、测试、`git status` 和 `git diff`。
2. 只更新 `docs/CURRENT_PROGRESS.md`。
3. 记录真实完成项和实际验证结果。
4. 保留失败或未验证的任务。
5. 删除过时的“下一步”，写出下一次最小任务。
6. 控制进度文件在约 100 行以内。
7. 不把详细教程、长期路线或协作规则复制到进度文件。
